pipeline {
  agent any

  stages {
    stage('Checkout') {
      steps {
        checkout scm
      }
    }

    stage('Build') {
      steps {
        echo 'Build step placeholder'
      }
    }

    stage('Test') {
      steps {
        echo 'Test step placeholder'
      }
    }
  }
}
